# Loki CYD — Custom Wordlist
#
# These credentials are IN ADDITION to the 72 built-in combinations.
# Built-in: 6 users (admin,root,user,guest,pi,Administrator) x 10 passwords + blank/same-as-user.
# Only add target-specific or unusual credentials here.
#
# Format: username:password
# Lines starting with # are comments.
#
# Example: if you know a target uses "operator:changeme"
# operator:changeme
