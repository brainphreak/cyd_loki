# Loki CYD — Custom Wordlist
#
# These credentials are IN ADDITION to the 242 built-in combinations.
# The firmware already tries 11 usernames x 20 passwords automatically.
# Only add target-specific or unusual credentials here.
#
# Format: username:password
# Lines starting with # are comments.
#
# Example: if you know a target uses "operator:changeme"
# operator:changeme
